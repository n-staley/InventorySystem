This class project was to make an inventory management system that allowed the user to add, modify, and delete parts
and products that are available. The program does not have a save feature, which makes it so work is lost when the
program is ended.